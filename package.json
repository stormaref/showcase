{
  "name": "showcase",
  "private": true,
  "scripts": {
    "dev:web": "npm run dev --prefix webapp",
    "build:web": "npm run build --prefix webapp",
    "lint:web": "npm run lint --prefix webapp"
  }
}
